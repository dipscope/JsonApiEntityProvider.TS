import { CustomKey } from '@dipscope/type-manager';

/**
 * Json api resource type key.
 * 
 * @type {CustomKey<string|undefined>}
 */
export const jsonApiResourceTypeKey: CustomKey<string|undefined> = new CustomKey<string|undefined>('$DSJsonApiResourceType');
